FROM activitytrackeracr.azurecr.io/activity-tracker-backend:v9

# Copy the updated server file with UUID fixes
COPY test-server.js ./

# Keep the same user and port configuration
USER nextjs
EXPOSE 3001

CMD ["node", "test-server.js"]
