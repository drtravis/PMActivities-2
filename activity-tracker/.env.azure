# Azure Production Environment Configuration
# This file contains the environment variables for Azure deployment
# Copy these values to your Azure App Service Configuration

# IMPORTANT: Replace all placeholder values with your actual Azure resource names!

# Environment
NODE_ENV=production

# Application Ports (Azure uses PORT environment variable)
PORT=8080

# Database Configuration (Azure Database for MySQL)
# Replace 'your-mysql-server' with your actual Azure MySQL server name
DB_TYPE=mysql
DB_HOST=your-mysql-server.mysql.database.azure.com
DB_PORT=3306
DB_USERNAME=your-mysql-admin-user
DB_PASSWORD=your-mysql-password
DB_NAME=pmactivities
DB_SSL=true

# API Configuration (Update with your Azure App Service URLs)
# Replace 'your-backend-app' and 'your-frontend-app' with actual names
API_BASE_URL=https://your-backend-app.azurewebsites.net
NEXT_PUBLIC_API_URL=https://your-backend-app.azurewebsites.net/api
FRONTEND_URL=https://your-frontend-app.azurewebsites.net

# JWT Configuration (Generate new secure keys for production)
JWT_SECRET=your-super-secure-jwt-secret-for-production-make-it-very-long-and-random
JWT_EXPIRES_IN=7d

# File Upload Configuration
UPLOAD_DEST=./uploads
MAX_FILE_SIZE=10485760

# Email Configuration (Configure with your email service)
EMAIL_ENABLED=true
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
EMAIL_FROM_NAME=PMActivities
EMAIL_FROM_ADDRESS=noreply@your-domain.com

# CORS Configuration (CRITICAL for frontend-backend communication)
# Add ALL possible frontend URLs separated by commas
CORS_ORIGIN=https://your-frontend-app.azurewebsites.net,https://your-custom-domain.com

# Organization Configuration
DEFAULT_ORG_NAME=PMActivities Organization

# Authentication
BCRYPT_ROUNDS=12
SESSION_TIMEOUT=86400000

# Security
CORS_ORIGIN=https://your-frontend-app.azurewebsites.net,https://your-custom-domain.com

# Logging
LOG_LEVEL=info
LOG_FORMAT=combined
LOG_FILE_ENABLED=true
LOG_FILENAME=app.log
LOG_MAX_SIZE=10m
LOG_MAX_FILES=5

# Cache (Optional - can enable Redis if needed)
CACHE_ENABLED=false
CACHE_TYPE=memory
CACHE_TTL=300

# Azure-specific configurations
WEBSITE_NODE_DEFAULT_VERSION=18.17.0
SCM_DO_BUILD_DURING_DEPLOYMENT=true
